export interface Curso {
    nombre: string;
    seccion: string;
    sala: string;
    foto: string;
    id: string;
}

export interface User{
    uid: string,
    email: string,
    password: string,
    name: string
}